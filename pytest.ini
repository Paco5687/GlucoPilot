[pytest]
# Put the repo root on sys.path so `import server...` works under plain `pytest`
# (CI runs `pytest`, not `python -m pytest`).
pythonpath = .
testpaths = tests
markers =
    risk_critical: clinical data integrity, migration, backup, and evidence regressions
