# Lean, high-signal linting — real bugs (pyflakes) over style bikeshedding.
line-length = 120
target-version = "py312"

[lint]
select = ["E9", "F"]  # syntax errors, undefined names, unused imports, f-string issues
